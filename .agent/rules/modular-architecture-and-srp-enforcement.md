---
trigger: always_on
---

Strictly adhere to the Single Responsibility Principle (SRP) and the specified folder structure. All new business logic must be generated within the '/src/services' directory. API entry/exit logic must reside only in '/src/controllers' (or handlers).
