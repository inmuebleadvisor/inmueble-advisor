---
trigger: always_on
---

When deploying or packaging, always use declarative configuration (Dockerfiles for service packaging and Kubernetes/Terraform configuration for cloud deployment). The agent must prioritize generating Infrastructure as Code (IaC) over manual setup steps.
