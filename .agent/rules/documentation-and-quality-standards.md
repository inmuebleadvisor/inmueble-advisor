---
trigger: always_on
---

Every new function, class, or method must include complete docstrings or comments explaining its purpose, parameters, and return type. Code must adhere to the project's official code style guide (e.g., PEP 8 for Python or ESLint configuration for JavaScript).